import DarkNavbar from "./DarkNavbar"
import LightNavbar from "./LightNavbar"

const Navbar = () => {
    return (
      <header>
        <DarkNavbar />
        <LightNavbar />
      </header>
    );
}

export default Navbar;